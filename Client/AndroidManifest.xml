<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.loveyu.remotecontrol"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ActivityStart"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ActivityMain"
            android:label="@string/title_activity_main"
            android:launchMode="singleTask" >
        </activity>
        <activity
            android:name=".ActivityLogin"
            android:label="@string/login" />
        <activity
            android:name=".ActivitySetting"
            android:label="@string/menu_settings"
            android:launchMode="singleTask" />
        <activity
            android:name=".ActivityDownload"
            android:label="@string/download_manager"
            android:launchMode="singleTask" >
        </activity>
        <activity
            android:name=".ActivityFile"
            android:label="@string/menu_file_manager"
            android:launchMode="singleTask" >
        </activity>
        <activity
            android:name=".ActivityTask"
            android:label="@string/menu_task_manager"
            android:launchMode="singleTask" >
        </activity>
        <activity
            android:name=".ActivityTerminal"
            android:label="@string/menu_run_terminal"
            android:launchMode="singleTask" >
        </activity>
        <activity
            android:name=".ActivityHelp"
            android:label="@string/help"
            android:launchMode="singleTask" >
        </activity>
    </application>

</manifest>